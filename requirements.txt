pandas>=2.0
numpy>=1.24
matplotlib>=3.8,<4
mplcursors>=0.5
seaborn>=0.13
pyarrow>=14
customtkinter>=5.2
pillow>=10
certifi>=2024.0  # bundled CA certs so HTTPS works in the frozen exe (see netutil.py)
tkinterdnd2>=0.4  # drag-and-drop CSV import (optional; app falls back if absent)
