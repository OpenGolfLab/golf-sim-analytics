"""Live shot tracking — polls GSPro's currentRound.dat (see
config.GSPRO_ROUND_FILE) instead of the CSV export button, so shots show
up in this app as they're hit rather than after a manual export/ingest.
"""
